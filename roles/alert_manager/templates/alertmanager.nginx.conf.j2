server {
  listen 80;
  server_name {{ alertmanager_host_name }};

  root /usr/share/nginx/html;
  index index.html index.htm;

  location / {
    proxy_pass http://localhost:9093/;

    auth_basic "Prometheus";
    auth_basic_user_file ".htpasswd";
  }
}